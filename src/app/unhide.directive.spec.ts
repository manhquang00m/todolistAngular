import { UnhideDirective } from './unhide.directive';

describe('UnhideDirective', () => {
  it('should create an instance', () => {
    const directive = UnhideDirective;
    expect(directive).toBeTruthy();
  });
});
